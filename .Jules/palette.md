## 2024-05-22 - Missing ARIA Labels on Icon-Only Buttons
**Learning:** Icon-only buttons (like social links) consistently lacked `aria-label` attributes, making them inaccessible to screen readers.
**Action:** Always check icon-only components for `aria-label` or `title` attributes during development and code reviews.
