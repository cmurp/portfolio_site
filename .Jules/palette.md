## 2024-05-23 - Accessibility of Icon Links
**Learning:** Icon-only links (like social media icons) are often missed by screen readers if they lack `aria-label`.
**Action:** Always add `aria-label` to `<a>` tags that wrap icons and have no visible text.
